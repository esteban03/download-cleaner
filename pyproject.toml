[project]
name = "download-sort"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "typer>=0.21.1",
    "watchdog>=6.0.0",
]


[project.scripts]
download-cleaner = "download_cleaner.main:app"